{
  "name": "gtabs",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "package": "cd dist && zip -r ../gtabs-extension.zip . && cd .. && echo 'Packaged → gtabs-extension.zip'",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.38",
    "@vitest/coverage-v8": "^2.1.0",
    "esbuild": "^0.24.0",
    "jsdom": "^29.0.1",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
